<% provide(:title, @user.name) %>
<%= link_to "Update Profile", edit_user_path(current_user), class: "btn btn-success" %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>